# Secrets: ignore everything under any secrets/ dir except *.example
secrets/*
!secrets/*.example
*.conf.local

# Star video pipeline cache (full YouTube sources)
tools/build/

# QA harness outputs (regenerable screenshots / device captures)
qa/out/
